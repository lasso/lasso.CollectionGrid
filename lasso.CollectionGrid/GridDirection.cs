namespace lasso.CollectionGrid
{
    public enum GridDirection
    {
        /// <summary>
        /// Places elements from left to right, then onto the next row
        /// </summary>
        Horizontal,

        /// <summary>
        /// Places elements from top to bottom, then onto the next column
        /// </summary>
        Vertical
    }
}